#include <iostream>
#include <chrono>
#include "stuff.hpp"

using namespace std;

int main(int argc, char const *argv[]) {
    uint32_t n = 10;
    uint64_t result = square(10);
    cout << result << endl;

    return 0;
}
